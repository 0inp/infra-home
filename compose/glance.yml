name: glance

services:
  glance:
    container_name: glance
    image: glanceapp/glance
    restart: unless-stopped
    ports:
      - 8080:8080
    volumes:
      - ../configs/glance:/app/config
    networks:
      - infra-network

networks:
  infra-network:
    external: true
